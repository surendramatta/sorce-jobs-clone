const { autoApplyToJobs, getAutoApplySettings, updateAutoApplySettings } = require('../services/autoApplyService');\n\nconst getSettings = async (req, res) => {\n  try {\n    const result = await getAutoApplySettings(req.user.id);\n    if (result.success) {\n      res.json(result);\n    } else {\n      res.status(400).json(result);\n    }\n  } catch (error) {\n    res.status(500).json({ message: error.message });\n  }\n};\n\nconst updateSettings = async (req, res) => {\n  try {\n    const result = await updateAutoApplySettings(req.user.id, req.body);\n    if (result.success) {\n      res.json(result);\n    } else {\n      res.status(400).json(result);\n    }\n  } catch (error) {\n    res.status(500).json({ message: error.message });\n  }\n};\n\nconst triggerAutoApply = async (req, res) => {\n  try {\n    const result = await autoApplyToJobs(req.user.id);\n    if (result.success) {\n      res.json(result);\n    } else {\n      res.status(400).json(result);\n    }\n  } catch (error) {\n    res.status(500).json({ message: error.message });\n  }\n};\n\nconst getAutoApplyStatus = async (req, res) => {\n  try {\n    const { AutoApplySettings } = require('../models');\n    const settings = await AutoApplySettings.findOne({\n      where: { userId: req.user.id }\n    });\n\n    if (!settings) {\n      return res.json({\n        success: true,\n        data: {\n          isEnabled: false,\n          appliedCount: 0,\n          lastAutoApplyDate: null\n        }\n      });\n    }\n\n    res.json({\n      success: true,\n      data: {\n        isEnabled: settings.isEnabled,\n        appliedCount: settings.appliedCount,\n        lastAutoApplyDate: settings.lastAutoApplyDate,\n        applicationLimit: settings.applicationLimit\n      }\n    });\n  } catch (error) {\n    res.status(500).json({ message: error.message });\n  }\n};\n\nmodule.exports = {\n  getSettings,\n  updateSettings,\n  triggerAutoApply,\n  getAutoApplyStatus\n};\n"
