const express = require('express');\nconst router = express.Router();\nconst emailController = require('../controllers/emailController');\nconst { auth } = require('../middleware/auth');\n\n// Email routes\nrouter.post('/send-verification', emailController.sendVerificationEmail);\nrouter.post('/send-status-update', auth, emailController.sendStatusUpdateEmail);\nrouter.post('/send-recommendation', auth, emailController.sendRecommendationEmail);\nrouter.get('/logs', auth, emailController.getEmailLogs);\n\nmodule.exports = router;\n"
