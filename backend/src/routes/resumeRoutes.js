const express = require('express');\nconst router = express.Router();\nconst resumeController = require('../controllers/resumeController');\nconst { auth, isJobSeeker } = require('../middleware/auth');\nconst upload = require('../utils/fileUpload');\n\n// Protected routes (job seekers only)\nrouter.post('/upload', auth, isJobSeeker, upload.single('resume'), resumeController.uploadResume);\nrouter.get('/', auth, isJobSeeker, resumeController.getUserResumes);\nrouter.get('/:id', auth, isJobSeeker, resumeController.getResumeById);\nrouter.delete('/:id', auth, isJobSeeker, resumeController.deleteResume);\nrouter.put('/:id/set-primary', auth, isJobSeeker, resumeController.setPrimaryResume);\n\nmodule.exports = router;\n"
