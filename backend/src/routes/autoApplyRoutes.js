const express = require('express');\nconst router = express.Router();\nconst autoApplyController = require('../controllers/autoApplyController');\nconst { auth, isJobSeeker } = require('../middleware/auth');\n\n// Auto-apply routes (job seekers only)\nrouter.get('/settings', auth, isJobSeeker, autoApplyController.getSettings);\nrouter.put('/settings', auth, isJobSeeker, autoApplyController.updateSettings);\nrouter.post('/apply-now', auth, isJobSeeker, autoApplyController.triggerAutoApply);\nrouter.get('/status', auth, isJobSeeker, autoApplyController.getAutoApplyStatus);\n\nmodule.exports = router;\n"
