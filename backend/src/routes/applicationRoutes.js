const express = require('express');\nconst router = express.Router();\nconst applicationController = require('../controllers/applicationController');\nconst { auth, isJobSeeker } = require('../middleware/auth');\n\n// Job seeker routes\nrouter.post('/apply', auth, isJobSeeker, applicationController.applyToJob);\nrouter.get('/', auth, applicationController.getUserApplications);\nrouter.get('/:id', auth, applicationController.getApplicationById);\nrouter.put('/:id/withdraw', auth, isJobSeeker, applicationController.withdrawApplication);\n\n// Employer routes\nrouter.put('/:id/status', auth, applicationController.updateApplicationStatus);\n\nmodule.exports = router;\n"
