const express = require('express');\nconst router = express.Router();\nconst jobController = require('../controllers/jobController');\nconst { auth, isEmployer, isJobSeeker } = require('../middleware/auth');\n\n// Public routes\nrouter.get('/', jobController.getAllJobs);\nrouter.get('/:id', jobController.getJobById);\nrouter.get('/search/filter', jobController.searchJobs);\n\n// Employer routes\nrouter.post('/', auth, isEmployer, jobController.createJob);\nrouter.put('/:id', auth, isEmployer, jobController.updateJob);\nrouter.delete('/:id', auth, isEmployer, jobController.deleteJob);\nrouter.get('/:id/applications', auth, isEmployer, jobController.getJobApplications);\n\nmodule.exports = router;\n"
